"""Vendored SASRec core, copied from the sibling repo `sasrec-pytorch`.

This is the faithful reproduction (model, data pipeline, baseline loss, evaluation).
sasrec-longseq builds its efficiency / longer-sequence experiments on top of it.
Kept vendored (not a dependency) so this repo clones and runs standalone.
"""
